8月25日：
今天stlink遇到了大问题，我的stlink插入会被识别为未知设备并且有黄色感叹号，在跟ai聊了好久并且尝试过许多办法之后，终于能正常识别设备了。
我本想先烧写代码模板看看车子能不能亮，结果录进去之后，按下车子开关，什么变化也没有。经过数次修改，车灯终于成功闪烁。

08‑26
今日工作内容
1. GPIO驱动SG90舵机、互斥锁实验
编写两个任务线程，使用互斥锁对舵机操作进行独占访问，模拟高、低优先级任务抢占场景，观察加锁前后舵机执行行为差异。
遇到HiBurn烧录报错no valid com问题，排查串口驱动、USB数据线、串口占用、BOOT+RST烧录按键时序，成功将Hi3861_wifiiot_app_allinone.bin镜像烧录进Hi3861开发板，验证舵机转动现象。
理解SG90舵机PWM脉冲控制角度，不同占空比对应不同转角。
2. STM32F103小车电机调试
- 调试STM32小车电机驱动，发现原代码仅修改内存变量AIN/BIN，没有输出到实际GPIO引脚，导致车辆无法后退。
- 修改`motor.c`，完善`Set_Pwm()`函数：实现正数前进、负数后退，绝对值控制PWM速度；完成方向引脚PB13、PB14电平控制；对PWM数值做限幅处理。
- 在主循环编写测试逻辑，测试全速前进、停止、慢速后退、慢速前进整套动作，验证电机正反转与调速功能。
- 学习STM32 USART串口通信原理，了解串口中断接收、自动成帧驱动框架，掌握串口初始化、printf打印输出，完成串口收发打印实验，利用串口打印调试信息辅助定位硬件软件bug。
收获总结
1. 掌握OpenHarmony LiteOS‑M互斥锁的创建、获取、释放、删除整套API，理解多线程资源竞争问题与互斥锁解决思路。
2. 掌握Hi3861编译产物区分，知道`allinone.bin`是业务实验使用的烧录镜像，区分boot、loader、OTA各类bin文件的用途。
3. STM3电机驱动加深理解：电机控制分为PWM速度信号 + GPIO方向信号两部分；负数参数需要软件内部取绝对值输出PWM，同时翻转方向引脚电平。
4. 熟悉STM32串口通信，学会使用串口打印作为调试手段。


8月27
今日完成工作
1. 补全 nfc.h 头文件
根据 nfc.c 和 main.c 中对 NFC 模块的调用，补全了 nfc.h，包括：
函数声明：NFC_Init()、NFC_Handler()、FoundCard_Handler()、FlowLight_Update() 等
全局变量声明：NFC_WakeUp_Ok、NFC_find_Card、NFC_sendcmd_find、NFC_DataBlock[4]、led_flow_active
已知卡 UID 宏定义（3 张卡）
2. 修复 USART 相关问题
排查并修复了串口初始化、收发函数中的问题，确保 USART1（调试打印）和 USART2（PN532 通信）正常工作。
3. 实现 NFC 刷卡触发流水灯
在 nfc.c 中实现：
FoundCard_Handler()：刷卡成功后启动右灯绿色流水灯（3 圈）
FlowLight_Update()：非阻塞流水灯状态机，每 100ms 推进一帧
led_flow_active 标志位：流水灯运行期间暂停寻卡，结束后自动恢复
4. 修复串口调试助手无条件刷屏问题
问题：control_system.c 的 System_Control() 中 printf 每 100ms 无条件打印电机调试信息（ph0 L0 R0 tg560/560 MA0 MB0...），导致串口助手一直弹数字。
解决：将该 printf 三行注释掉，只保留贴卡时的寻卡响应打印（put_HEX）。
5. 修复贴卡不亮灯、串口无数据的问题
排查发现两个根因：
根因 A — PN532 未被唤醒（nfc.c）：
NFC_Handler 只有 "唤醒成功后才发寻卡指令" 的分支，但没有发送唤醒命令的代码。NFC_WakeUp_Ok 初始为 0，永远等不到唤醒应答，寻卡指令永不发送。
解决：在 NFC_Handler 中加入未唤醒分支，用静态计数器每 500ms 自动发送一次唤醒命令 NFC_WakeUp，直到中断检测到唤醒应答。
根因 B — skip=30 吞掉寻卡响应（stm32f10x_it.c）：
唤醒成功后设置 skip = 30（丢弃后续 30 字节），但 PN532 唤醒应答仅约 16 字节，skip 剩余约 14 字节会把寻卡响应的前 14 字节也丢掉，导致 RxCounter 永远到不了 25，贴卡不触发。而 nfc.c 发寻卡命令前已有 RxCounter=0 重置，skip 完全多余。
解决：删除整个 skip 逻辑（变量声明、两处赋值、else if(skip>0) 分支）。


8月31日
今日开展Hi3861综合实验相关学习，梳理TCRT5000红外循迹实验代码，完成代码提取与功能解析。研读综合实验1任务要求，明确需要联动红外对管、超声波、舵机、蓝牙、UART，结合RTOS内核实现多任务调度：舵机左右旋转测距；前15秒红外对管寻线，15秒后开启蓝牙通信；任务3打印消息队列，任务1、2交替运行。
整理实验模块BUILD.gn配置，为后续综合工程编写做准备。

9月1日
今日完成了通信协议的任务，小车可以前进1s之后任意转弯。然而在使小车红外检测到桌子边缘即停止转弯这一任务遇到困难没有解决，一旦解决立刻补齐

9月2日
今日终于完成了利用蓝牙遥控小车的任务。其中遇到了很多困难比如小车右轮紧左轮松，uart2初始化失败等等，经过很久的调整纠错之后最终完成了。可以在手机蓝牙调试器app实现发信息控制小车前进方向和速度
